"""
Console-less entry point for DeskGifs.

Run this with `pythonw` (or just double-click it) so no terminal window
appears. All the actual logic lives in desk_gif.py.
"""

from desk_gif import main

if __name__ == "__main__":
    main()
